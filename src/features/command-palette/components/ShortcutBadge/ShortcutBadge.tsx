/**
 * Re-export ShortcutBadge from shared.
 *
 * This component was moved to @/shared/components/ShortcutBadge
 * so it can be used across features without module boundary violations.
 */

export { ShortcutBadge } from '@/shared/components/ShortcutBadge/ShortcutBadge';
