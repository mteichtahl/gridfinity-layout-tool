import { describe, it, expect } from 'vitest';
import { PathEditOverlay3D } from './PathEditOverlay3D';

describe('PathEditOverlay3D', () => {
  it('exports a function', () => {
    expect(typeof PathEditOverlay3D).toBe('function');
  });
});
