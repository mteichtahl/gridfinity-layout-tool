import { describe, it, expect } from 'vitest';
import { PathDrawingPreview3D } from './PathDrawingPreview3D';

describe('PathDrawingPreview3D', () => {
  it('exports a function', () => {
    expect(typeof PathDrawingPreview3D).toBe('function');
  });
});
