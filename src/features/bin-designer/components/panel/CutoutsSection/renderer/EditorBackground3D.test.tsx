import { describe, it, expect } from 'vitest';
import { EditorBackground3D } from './EditorBackground3D';

describe('EditorBackground3D', () => {
  it('exports a function', () => {
    expect(typeof EditorBackground3D).toBe('function');
  });
});
