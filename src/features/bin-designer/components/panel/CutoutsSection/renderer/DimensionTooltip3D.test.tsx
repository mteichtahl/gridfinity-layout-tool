import { describe, it, expect } from 'vitest';
import { DimensionTooltip3D } from './DimensionTooltip3D';

describe('DimensionTooltip3D', () => {
  it('exports a function', () => {
    expect(typeof DimensionTooltip3D).toBe('function');
  });
});
