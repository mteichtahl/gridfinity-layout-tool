import { describe, it, expect } from 'vitest';
import { CutoutCanvas3D } from './CutoutCanvas3D';

describe('CutoutCanvas3D', () => {
  it('exports a function', () => {
    expect(typeof CutoutCanvas3D).toBe('function');
  });
});
