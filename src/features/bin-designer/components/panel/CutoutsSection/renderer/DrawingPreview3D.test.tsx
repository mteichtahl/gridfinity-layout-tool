import { describe, it, expect } from 'vitest';
import { DrawingPreview3D } from './DrawingPreview3D';

describe('DrawingPreview3D', () => {
  it('exports a function', () => {
    expect(typeof DrawingPreview3D).toBe('function');
  });
});
