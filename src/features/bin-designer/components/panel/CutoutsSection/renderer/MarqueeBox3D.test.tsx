import { describe, it, expect } from 'vitest';
import { MarqueeBox3D } from './MarqueeBox3D';

describe('MarqueeBox3D', () => {
  it('exports a function', () => {
    expect(typeof MarqueeBox3D).toBe('function');
  });
});
