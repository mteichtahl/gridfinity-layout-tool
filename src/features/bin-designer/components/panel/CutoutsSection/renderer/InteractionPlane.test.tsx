import { describe, it, expect } from 'vitest';
import { InteractionPlane } from './InteractionPlane';

describe('InteractionPlane', () => {
  it('exports a function', () => {
    expect(typeof InteractionPlane).toBe('function');
  });
});
