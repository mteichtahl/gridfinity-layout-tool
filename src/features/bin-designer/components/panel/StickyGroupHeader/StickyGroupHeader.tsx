/**
 * Re-export shared StickyGroupHeader for bin-designer backward compatibility.
 *
 * The canonical implementation lives in @/shared/components/StickyGroupHeader.
 */

export { StickyGroupHeader } from '@/shared/components/StickyGroupHeader';
