import { describe, it, expect } from 'vitest';

import { BinNameLabel } from './BinNameLabel';

describe('BinNameLabel', () => {
  it('exports a component function', () => {
    expect(typeof BinNameLabel).toBe('function');
  });
});
