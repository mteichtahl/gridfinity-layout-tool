import { describe, it, expect } from 'vitest';

import { BinAxisLabels } from './BinAxisLabels';

describe('BinAxisLabels', () => {
  it('exports a component function', () => {
    expect(typeof BinAxisLabels).toBe('function');
  });
});
