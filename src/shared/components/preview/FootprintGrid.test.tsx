import { describe, it, expect } from 'vitest';

import { FootprintGrid } from './FootprintGrid';

describe('FootprintGrid', () => {
  it('exports a component function', () => {
    expect(typeof FootprintGrid).toBe('function');
  });
});
