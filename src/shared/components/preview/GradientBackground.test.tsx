import { describe, it, expect } from 'vitest';

import { GradientBackground } from './GradientBackground';

describe('GradientBackground', () => {
  it('exports a component function', () => {
    expect(typeof GradientBackground).toBe('function');
  });
});
